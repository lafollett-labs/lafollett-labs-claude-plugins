# code-reviewer

Lean, pragmatic code reviews with scope discipline. Uses right-sized approach — primary agent handles small changes directly, sub-agents for larger reviews.

## Features

- **Right-sized reviews** — < 200 lines: primary agent; ≥ 200 lines: stack-specific PE sub-agents
- **Built-in PE sub-agents** — `pe-go` (Go/SQL/Lambda), `pe-vue` (Vue/Nuxt/TS), `pe-aws-infra` (CDK/CDKTF/IaC), `pe-governance` (agent definitions/skills/CLAUDE.md), `pe-devtools` (local dev tooling, single-operator threat model), each with `claude-opus-4-7` and a self-contained four-pass protocol
- **Scope discipline** — In Scope (blocks PR) vs Out of Scope (tech debt)
- **Finding validation** — All CRITICAL/HIGH/MEDIUM verified before publishing
- **Multiple review types** — Branch diff, PR review, staged changes, specific files, ad-hoc
- **Branch discipline** — Auto-checkouts PR source branch, commits review to working branch
- **Multi-round reviews** — Appends findings to existing review files instead of overwriting
- **PR comment + approval** — Prompts reviewer to post summary comment and approve/request changes
- **Generic fallback** — Stacks not covered by a built-in PE (Rust, Python, Java, C#, etc.) get a generic three-pass review using the project's Stack Map test commands

## Triggers

This skill auto-activates when you mention:

- Code review, PR review
- Review changes, review my branch
- Review staged changes, review these files

## Review Types

| Request | What Gets Reviewed |
| ------- | ------------------ |
| "Review my branch" | `git diff main...HEAD` |
| PR URL or "Review PR #17" | Fetch source branch, diff against target |
| "Review staged changes" | `git diff --cached` |
| "Review these files: X, Y" | Entire file contents |
| "Review this code" + paste | The provided code |

## Built-in PE Sub-Agents

Five stack-specific PE agents ship with the plugin. Each runs the full four-pass protocol (Architecture → Quality+Tests → Security → mandatory Adversarial Re-read) within its domain and returns findings as YAML for the parent skill to consolidate.

| Agent | Domain | Test commands |
| ----- | ------ | ------------- |
| `code-reviewer:pe-go` | Go / PostgreSQL / AWS Lambda | `go vet`, `go test -race`, `staticcheck` |
| `code-reviewer:pe-vue` | Vue 3 / Nuxt 3 / TS / Tailwind / Storybook | `npm run typecheck`, `npm test`, `npm run build-storybook` |
| `code-reviewer:pe-aws-infra` | AWS CDK / Cloudflare CDKTF / Terraform / Docker / GH Actions | `npm test`, `npx cdk synth`, `actionlint` |
| `code-reviewer:pe-governance` | Agent definitions, skills, CLAUDE.md (audience-is-the-model markdown) | grep-based lint, frontmatter / pseudocode determinism checks |
| `code-reviewer:pe-devtools` | Local dev tooling — bash scripts, hooks, code-review wrappers (single-operator threat model) | `shellcheck`, smoke tests; calibrated against multi-tenant CI hardening |

The parent skill picks which PE(s) to dispatch from the project's Stack Map (`CLAUDE.md` or `.code-reviewer.yml`) — generated by `/init-project`.

## Scope Discipline

**In Scope** — Issues in changed code:

- Report all severities (CRITICAL → INFO)
- CRITICAL/HIGH block the PR

**Out of Scope** — Pre-existing issues:

- Note for awareness
- Don't block PR
- Log to backlog

## Severity Levels

| Level | Criteria | Action |
| ----- | -------- | ------ |
| 🔴 CRITICAL | Security vulnerabilities, data loss | Must fix |
| 🟠 HIGH | Bugs, significant design issues | Should fix |
| 🟡 MEDIUM | Code quality, maintainability | Consider |
| 🟢 LOW | Style, minor improvements | Optional |
| ℹ️ INFO | Observations | Awareness |

## Report Output

Review reports are written to `./docs/code-reviews/`. See SKILL.md Phase 6 for naming conventions.

## Usage Examples

### Branch Review

```text
You: "Review my branch"
Claude: [Analyzes diff, validates findings, generates report]
```

### Staged Changes

```text
You: "Review my staged changes"
Claude: [Reviews git diff --cached]
```

### PR Review

```text
You: "Review PR #984"
Claude: [Checks out source branch, reviews diff, writes/appends report, prompts for PR comment]
```

### Ad-hoc Review

```text
You: "Review this code" + paste code
Claude: [Reviews provided code]
```

---

*Part of [LaFollett Labs Claude Plugins](../../../../README.md)*
